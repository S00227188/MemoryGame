<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/gameOverTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Game Over!"
        android:textSize="40sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="100dp"
        android:layout_marginTop="100dp"/>

    <TextView
        android:id="@+id/scoreView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your score was: "
        android:textSize="28sp"
        app:layout_constraintTop_toBottomOf="@id/gameOverTitle"
        app:layout_constraintStart_toStartOf="@id/gameOverTitle"
        android:layout_marginTop="8dp"/>

    <Button
        android:id="@+id/playAgainButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play Again"
        android:textSize="17sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginEnd="100dp"
        android:layout_marginTop="100dp" />

    <Button
        android:id="@+id/saveScoreButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Score"
        android:textSize="17sp"
        app:layout_constraintTop_toBottomOf="@id/playAgainButton"
        app:layout_constraintEnd_toEndOf="@id/playAgainButton"
        android:layout_marginTop="16dp"/>

    <Button
        android:id="@+id/viewHighScoresButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="View Top 5 Highscores"
        android:textSize="17sp"
        app:layout_constraintTop_toBottomOf="@id/saveScoreButton"
        app:layout_constraintEnd_toEndOf="@id/saveScoreButton"
        android:layout_marginTop="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
